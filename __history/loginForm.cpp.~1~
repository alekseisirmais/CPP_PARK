//---------------------------------------------------------------------------

#include <fmx.h>
#pragma hdrstop

#include "loginUnit.h"
#include "User.h"
#include "Request.h"
#include "Usb.h"
//---------------------------------------------------------------------------
#pragma package(smart_init)
#pragma resource "*.fmx"
TForm1 *Form1;

User user;
//---------------------------------------------------------------------------
__fastcall TForm1::TForm1(TComponent* Owner)
	: TForm(Owner)
{
}
//---------------------------------------------------------------------------

bool log_in(AnsiString username, AnsiString password) {
	return true;  // ??? ???????????
}



void __fastcall TForm1::Button1Click(TObject *Sender)
{
	if (log_in(usernameField->Text, passwordField->Text)) {

	} else {
		ShowMessage("username or password is incorrect");
	}
}
//---------------------------------------------------------------------------

